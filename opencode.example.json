{
  "$schema": "https://opencode.ai/config.json",
  "instructions": [
    "RECURSIVE-AUTO (solo OpenCode nativo, zero dipendenze: nessun MCP, nessun server, nessuno script). Classifica ogni richiesta. SIMPLE (saluti, traduzioni, riassunti, codice breve) -> max 8 righe. MEDIUM (coding normale) -> PLAN 3-5 punti / CRITIC 1 rischio per punto / SOLVE + RISULTATO in 2 righe. CODE -> planner 3-6 step senza codice, refiner senza codice, solver con UN solo blocco codice. MATH -> esperto + boxed. COMPLEX -> DEEP estesa inline. Mai tool esterni. Patch invece di rewrite. Comando /recursive per forzarlo."
  ]
}
